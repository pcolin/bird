<Window x:Class="client.Views.VolatilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:client.Views"
        Icon="/Images/bird_32x32.ico"
        Title="Volatility Setting" Height="300" Width="300"
        Closing="Window_Closing">
    <TabControl x:Name="VolatilityTabControl" ItemsSource="{Binding TabItems}" SelectedItem="{Binding SelectedTab}">
        <TabControl.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Exchange}" />
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.ContentTemplate>
            <DataTemplate DataType="x:Type vm:VolatilityUserControlViewModel">
                <local:VolatilityUserControl />
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</Window>
