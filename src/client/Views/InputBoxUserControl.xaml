<UserControl x:Class="client.Views.InputBoxUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="68" d:DesignWidth="210">
    <Grid Background="Coral" Opacity="0.7">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Name="TextBlockValue" Margin="5" VerticalAlignment="Center">New volatility:</TextBlock>
        <TextBox Name="TextBoxValue" Margin="5" MaxWidth="110" Grid.Column="1" Grid.ColumnSpan="2"></TextBox>
        <Button Name="ButtonOK" Margin="5" MinWidth="50" Grid.Row="1" Grid.Column="1" Click="ButtonOK_Click">OK</Button>
        <Button Name="ButtonCancel" Margin="5" MinWidth="50" Grid.Row="1" Grid.Column="2" Click="ButtonCancel_Click">Cancel</Button>
    </Grid>
</UserControl>
